language: python
python:
    - "3.6"
before_install:
    - sudo apt-get update -qq
    - sudo apt-get build-dep -qq python-pygame
    - sudo apt-get install -qq python-pygame
script:
    - nosetests src/*.py
